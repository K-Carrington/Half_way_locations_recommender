<!DOCTYPE html>
<div class="page-header">

    <h1>Welcome <%= user.local.first_name || user.facebook.first_name %>!</h1>

    <a href="/logout" class="btn btn-default btn-sm">Logout</a>

</div>
<div class="row">
    <div class="col-sm-6 col-sm-offset-3">
        <div class="well">
          <% if(user.facebook.id) { %>
            <h3>Facebook User</h3>
          <% } else { %>
            <h3>Locally Stored User</h3>
          <%}%>
            <p>
                <strong>First</strong>: <%= user.local.first_name || user.facebook.first_name %><br>
                <strong>Last</strong>: <%= user.local.last_name || user.facebook.last_name %><br>
                <strong>Hometown</strong>: <%= user.facebook.hometown %><br>
                <strong>Id</strong>: <%= user._id %><br>
                <strong>Email</strong>: <%= user.local.email || user.facebook.email || "None" %><br>
                <strong>Password</strong>: <%= user.local.password || user.facebook.token %><br>
                <% if(user.facebook.id) { %>
                  <strong>Using Facebook</strong><br>
                <% }%>
                <a href="/profile/edit" class="btn btn-default btn-sm">Edit</a>
            </p>

        </div>
    </div>
</div>
